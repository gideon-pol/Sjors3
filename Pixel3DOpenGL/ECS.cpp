#include "ECS.h"

/*void ECS::AddObject(Object& object) {
	object.id = curId;
	//objects.insert({ curId, object });
	curId++;
}

void ECS::RemoveObject(Object& object) {
	//objects.erase(object.id);
}*/